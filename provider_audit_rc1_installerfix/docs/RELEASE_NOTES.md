# Release 5.0.5 RC1 — Provider Intelligence Audit

Adds Provider Diagnostics, trace IDs and timelines, a unified provider model, verified-provider-only recommendation guardrails, dependency inventory, architecture decisions, and regression tests. RC1 is an audit/scaffold release; it intentionally identifies the current static fallback without silently changing all production lookup paths.
