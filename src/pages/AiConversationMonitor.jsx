/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect, useState } from "react";
import { Activity, RefreshCw } from "lucide-react";
import { getAdvisorSessions } from "../services/aiAdvisorService";
export default function AiConversationMonitor(){const [data,setData]=useState({turns:[],decisions:[]});const [busy,setBusy]=useState(false);async function load(){setBusy(true);try{setData(await getAdvisorSessions());}finally{setBusy(false);}}useEffect(()=>{load();},[]);return <main className="ai004c-monitor"><header><div><span>AI-004C</span><h1>Conversation Monitor</h1><p>Recent advisor turns, routing decisions, lead scores, and customer facts.</p></div><button onClick={load} disabled={busy}><RefreshCw size={16}/> Refresh</button></header><section>{(data.turns||[]).map((turn,index)=><article key={`${turn.sessionId}-${index}`}><div><Activity size={17}/><strong>{turn.sessionId}</strong><span>{turn.stage}</span></div><p>{turn.response?.message}</p><dl><div><dt>Lead score</dt><dd>{turn.response?.leadScore ?? "—"}</dd></div><div><dt>Tools</dt><dd>{(turn.toolsInvoked||[]).join(", ")}</dd></div><div><dt>Known facts</dt><dd>{Object.keys(turn.memory?.facts||{}).join(", ")||"None"}</dd></div></dl></article>)}{!(data.turns||[]).length&&<p>No advisor sessions have been recorded since the backend started.</p>}</section></main>}
