import test from "node:test"; import assert from "node:assert/strict";
import { generateQuote, compareQuotePlans, saveQuote, getQuote, selectQuotePlan } from "../services/quoteIQ/index.js";
test("QuoteIQ generates a ranked Quote Ready record",()=>{ const q=generateQuote({customer:{currentProvider:"Spectrum",monthlyBill:115,needs:["workFromHome"],painPoints:["reliability"]},availableProviderIds:["att","spectrum"]}); assert.equal(q.status,"QUOTE_READY"); assert.equal(q.ranking.businessValueWeight,0.6); assert.ok(q.recommendation); assert.notEqual(q.recommendation.providerId,"spectrum"); });
test("QuoteIQ persists, compares, and versions selection",()=>{ const q=saveQuote(generateQuote({customer:{monthlyBill:100}})); assert.equal(getQuote(q.quoteId).quoteId,q.quoteId); assert.ok(compareQuotePlans(q,[q.plans[0].planId]).comparison.length===1); const selected=selectQuotePlan(q.quoteId,q.plans[0].planId); assert.equal(selected.status,"PLAN_SELECTED"); assert.equal(selected.version,2); });
